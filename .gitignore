# Generator lives in generator\ IN THIS REPO. It used to live outside version
# control, which is how it got lost and had to be rebuilt from the live site.
index_v5.html
panel_v5.h

# Firmware belongs to the firmware project
panel.h
Firmware/

# Retired with the /exec amputation
*.gs
set-client-id.ps1

# Local only, never published
_scratch/
_bak/
client_id.txt
*.zip
shot119.json
profiles_raw.json
